
#include "resources/foo.jc"

int main() {
    Byte * hello = "%d";
    Foo * foo = malloc(sizeof(Foo) * 2);
    for(int i = 0; i < 5; i++){
        printf(hello, i);
    }

    free(foo);
    return 0;
}

